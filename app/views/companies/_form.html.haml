= simple_form_for(@company) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :slogan
    = f.input :logo
    = f.input :clave

  .form-actions
    = f.button :submit
