= simple_form_for(@company) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :slogan
    = f.input :logo
    %h3
      Clave
      = @company.clave
    -#= f.input :clave
  .form-actions

    = f.button :submit
